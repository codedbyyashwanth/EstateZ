export type HeadingPropes = {
      heading: string;
      text: string;
      buttonText: string;
}

export type RealEstateProperty = {
      name: string;
      description: string;
      bedrooms: number;
      bathrooms: number;
      priceUSD: string;
      imageUrl: string;
};
